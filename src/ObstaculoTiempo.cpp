#include "ObstaculoTiempo.h"

ObstaculoTiempo::ObstaculoTiempo() {
		posicion = { 0,0 };
		velocidad = { 0,0 };
		aceleracion = { 0,0 };
		dim = { 0,0,0,0 };
		dano = -1; //falta funcion que regule el daño
}